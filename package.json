{
  "name": "@code-dot-org/svm",
  "description": "Code.org fork of karpathy/svmjs for use within the Code Studio learning environment.",
  "version": "0.1.0",
  "author": "Andrej Karpathy <andrej.karpathy@gmail.com>",
  "main": "./lib/svm",
  "repository": {
    "type": "git",
    "url": "http://github.com/code-dot-org/svmjs.git"
  },
  "scripts": {
    "test": "node test/testnode.js",
    "lint": "eslint lib/**",
    "pretest": "npm run lint",
    "preversion": "npm whoami && npm test",
    "postversion": "git push && git push --tags && npm publish"
  },
  "devDependencies": {
    "eslint": "^3.1.0"
  }
}
